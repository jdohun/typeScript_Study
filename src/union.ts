// 숫자 또는 문자열을 가질 수 있는 변수, 개수 제한 없음
let myVariable: number | string;

myVariable = 10;      // 숫자 할당 가능
console.log(myVariable);
myVariable = "Hello"; // 문자열 할당 가능
console.log(myVariable);
// myVariable = true;  // 에러: boolean은 허용되지 않음